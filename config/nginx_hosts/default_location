# This config will be attached to every server location config
#
# ...
# include /etc/nginx/vhost.d/default;
# location / {
# 	proxy_pass http://sandbox.loc;
# 	set $upstream_keepalive false;
# 	include /etc/nginx/vhost.d/default_location;  <----- here it is
# 	include /etc/nginx/vhost.d/sandbox.loc_location;
# }
# ...